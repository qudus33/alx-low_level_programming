#include <stdio.h>
#include "main.h"

/**
 * mul - function to multiply two numbers
 * @a: first number
 * @b: second number
 * Return: return value of c as the answer
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
