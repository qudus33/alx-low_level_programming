#include <string.h>
#include "main.h"

/**
 * _strlen - print the length of a string
 *
 * @s: argument s
 *
 * Return: lenght of string
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
