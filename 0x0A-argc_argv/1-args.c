#include <stdio.h>
#include <stdlib.h>

/**
 * main - print number of arguments
 * @argc: gets the argument passed
 * Return: 0 on success
 */

int main(int argc)
{
	printf("%d\n", argc);
	return (0);
}
